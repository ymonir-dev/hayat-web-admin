import { useEffect, useState } from 'react'; import { supabase } from '../lib/supabase'; import type { JsonMap } from '../types'; import { Badge, Card, Loading, PageTitle, Stat, Table } from '../components/ui';
export default function PlatformAdmin(){const [loading,setLoading]=useState(true),[error,setError]=useState(''),[overview,setOverview]=useState<JsonMap>({}),[hospitals,setHospitals]=useState<JsonMap[]>([]),[accounts,setAccounts]=useState<JsonMap[]>([]);async function load(){setLoading(true);setError('');const [o,h,a]=await Promise.all([supabase.rpc('hayat_platform_overview'),supabase.rpc('hayat_platform_hospitals'),supabase.rpc('hayat_platform_accounts')]);const bad=[o,h,a].find(x=>x.error);if(bad?.error)setError(bad.error.message);else{setOverview(o.data??{});setHospitals(h.data??[]);setAccounts(a.data??[])}setLoading(false)}useEffect(()=>{load()},[]);async function hospitalStatus(id:string,active:boolean){const {error}=await supabase.rpc('hayat_platform_set_hospital_active',{p_hospital_id:id,p_active:active});if(error)setError(error.message);else load()}async function accountStatus(id:string,status:string){const {error}=await supabase.rpc('hayat_platform_set_provider_status',{p_account_id:id,p_status:status});if(error)setError(error.message);else load()}if(loading)return <Loading/>;return <><PageTitle title="Hayat Platform Administration" subtitle="Global oversight across all hospitals and provider organizations." actions={<button className="btn btn-secondary" onClick={load}>Refresh</button>}/>{error&&<div className="error">{error}</div>}<div className="stats-grid"><Stat label="Hospitals" value={overview.total_hospitals}/><Stat label="Active hospitals" value={overview.active_hospitals}/><Stat label="Provider accounts" value={overview.provider_accounts}/><Stat label="Pending accounts" value={overview.pending_accounts}/><Stat label="Owners" value={overview.owners}/><Stat label="Branches" value={overview.branches}/></div><Card title="Hospitals / Clinics"><Table rows={hospitals} columns={[{key:'name',label:'Hospital'},{key:'is_active',label:'Status',render:r=><Badge value={r.is_active?'active':'inactive'}/>},{key:'branch_count',label:'Branches'},{key:'staff_count',label:'Staff'},{key:'owner_name',label:'Owner'},{key:'owner_email',label:'Owner email'},{key:'actions',label:'Action',render:r=><button className="btn btn-small btn-secondary" onClick={()=>hospitalStatus(String(r.id),!r.is_active)}>{r.is_active?'Deactivate':'Activate'}</button>}]} /></Card><Card title="Provider accounts"><Table rows={accounts} columns={[{key:'full_name',label:'Name'},{key:'email',label:'Email'},{key:'hospital_name',label:'Hospital'},{key:'role',label:'Role'},{key:'status',label:'Status',render:r=><Badge value={r.status}/>},{key:'actions',label:'Change',render:r=><select value={r.status} onChange={e=>accountStatus(String(r.id),e.target.value)}><option>pending</option><option>approved</option><option>suspended</option><option>rejected</option></select>}]} /></Card></>}

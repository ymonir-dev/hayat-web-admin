import { BrowserRouter, Navigate, Route, Routes } from 'react-router-dom'; import { AuthProvider, useAuth } from './auth'; import Shell from './components/Shell'; import Login from './pages/Login'; import Home from './pages/Home'; import OwnerDashboard from './pages/OwnerDashboard'; import AdminDashboard from './pages/AdminDashboard'; import ManagerDashboard from './pages/ManagerDashboard'; import AuditorDashboard from './pages/AuditorDashboard'; import StaffAccess from './pages/StaffAccess'; import GenericData from './pages/GenericData'; import SystemReadiness from './pages/SystemReadiness'; import PlatformAdmin from './pages/PlatformAdmin';
function Guard({children}:{children:React.ReactNode}){const a=useAuth();if(a.loading)return <div className="loading full">Loading Hayat…</div>;if(!a.platformAdmin&&!a.provider)return <Navigate to="/login" replace/>;return <>{children}</>}
function AppRoutes(){return <Routes><Route path="/login" element={<Login/>}/><Route element={<Guard><Shell/></Guard>}><Route path="/" element={<Home/>}/><Route path="/platform" element={<PlatformAdmin/>}/><Route path="/owner" element={<OwnerDashboard/>}/><Route path="/admin" element={<AdminDashboard/>}/><Route path="/manager" element={<ManagerDashboard/>}/><Route path="/audit" element={<AuditorDashboard/>}/><Route path="/staff" element={<StaffAccess/>}/><Route path="/branches" element={<GenericData kind="branches"/>}/><Route path="/doctors" element={<GenericData kind="doctors"/>}/><Route path="/appointments" element={<GenericData kind="appointments"/>}/><Route path="/services" element={<GenericData kind="services"/>}/><Route path="/requests" element={<GenericData kind="requests"/>}/><Route path="/notifications" element={<GenericData kind="notifications"/>}/><Route path="/hr" element={<GenericData kind="hr"/>}/><Route path="/readiness" element={<SystemReadiness/>}/></Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes>}
export default function App(){return <BrowserRouter><AuthProvider><AppRoutes/></AuthProvider></BrowserRouter>}
